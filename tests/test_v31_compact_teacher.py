from __future__ import annotations\n\nimport unittest\n\nfrom scripts.generate_v31_local_teacher_labels import (\n    _extract_json,\n    _validate_compact_judgments,\n)\n\n\nclass V31CompactTeacherTests(unittest.TestCase):\n    def test_compact_schema_decodes_to_v3_judgment(self) -> None:\n        result = {"j": [[0, "s", 4, 3, 95, "n"], [1, "a", 3, 2, 88, "u"]]}\n        judgments = _validate_compact_judgments(\n            result,\n            ["กล่าว", "เงียบ"],\n        )\n\n        self.assertEqual(judgments["กล่าว"]["relation"], "synonym")\n        self.assertEqual(judgments["กล่าว"]["confidence"], 0.95)\n        self.assertEqual(judgments["เงียบ"]["relation"], "antonym")\n        self.assertEqual(judgments["เงียบ"]["replaceability"], 0)\n\n    def test_compact_schema_requires_every_candidate(self) -> None:\n        with self.assertRaisesRegex(ValueError, "Missing compact candidate ids"):\n            _validate_compact_judgments(\n                {"j": [[0, "n", 3, 2, 90, "l"]]},\n                ["งาม", "สะสวย"],\n            )\n\n    def test_extract_json_accepts_wrapped_text(self) -> None:\n        parsed = _extract_json(\n            'prefix {"j":[[0,"x",2,0,80,"n"]]} suffix'\n        )\n        self.assertEqual(parsed["j"][0][1], "x")\n\n\nif __name__ == "__main__":\n    unittest.main()\n